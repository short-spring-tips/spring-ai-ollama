# Run Offline LLMs with Spring AI and Ollama

Java 25, Spring Boot 4.1.0, and Spring AI 2.0.0. Spring Boot discovers `compose.yml`, starts DaShaun's preloaded `phi4-mini` Ollama image, waits for readiness, and wires `ChatClient` to the local service.

Start the application in the first terminal and leave it running:

```bash
./mvnw -q spring-boot:run
```

Then open a second terminal and call the live endpoint with HTTPie:

```bash
http :8080/explain topic==Spring
```

The response is generated by the containerized `phi4-mini` model. Return to the first terminal and press `Ctrl+C` when you are finished.

The application sends no prompt to a cloud provider and needs no cloud API key.

Video: `https://youtube.com/shorts/JH9L43NzWOM`
